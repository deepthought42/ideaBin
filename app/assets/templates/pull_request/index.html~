
<div class="col-md-12" ng-controller="PullRequestIndexController">
	<div ng-repeat="pullRequest in pullRequests">
		<span class="col-md-9" 
					ng-click="showPullRequest(pullRequest.id)">
			{{pullRequest.message}}
		</span>
		<span class="col-md-3 pull-right">
			<span class="fa fa-edit" ng-click="acceptPullRequest(pullRequest.id)"></span>
			<span class="fa fa-times" ng-click="rejectPullRequest(pullRequest)"></span>
		<span>
	</div>
	

</div>
