<div id="ideaCommentsListWrapper" ng-controller="ResourceCommentIndexController">
  <ul id="ideaCommentsList" ng-repeat="resourceComment in resourceComments">
    <li class="resourceCommentsListItem col-md-12">
      <div class="ideaCommentWrapper col-md-9">
				<span class="ideaComment col-md-12" ng-click="editComment(resourceComment)">
   				{{ resourceComment.comment }}
  			</span>
  			<span id="resourceCommentTimestamp" class="col-md'12">
					{{resourceComment.created_at | date:'shortDate'}} at {{resourceComment.created_at | date:'shortTime'}}
				</span>
    		
			</div>
<div ng-click="updateComment(resourceComment)" class="fa fa-pencil col-md-1 pull-left resourceEditIcon"></div>     
			<div class="col-md-2 pull-right">  
				
			  <img ng-src="{{$session.user.avatar_file_name ? '/images/'+$session.user.id +'/thumb/' + $session.user.avatar_file_name : '/images/missing.png'}}" />
			</div>
    </li>
  </ul>
  <div class="col-md-12" ng-controller="ResourceCommentCreationController">
    <form novalidate> 
      <textarea rows="5" cols="100" ng-model="resource_comment.message" placeholder="Create Resource Comment Here" class="form-control"></textarea>
	<button id="createResourceCommentButton" ng-click="createResourceComment()" class="btn btn-default btn-lg">
          Save Resource Comment
        </button>
    </form>
  </div>
</div>
